<%- include('partials/header') %>

<div class="container">
  <section class="hero" style="padding-bottom:2rem">
    <p class="hero-label">ADMIN</p>
    <h1 class="hero-title" style="font-size:2rem">Subscriptions</h1>
  </section>

  <div style="display:flex;gap:1rem;margin-bottom:2rem">
    <a href="/admin/settings" class="btn btn-primary">⚙ Payment Settings</a>
    <a href="/admin" class="btn btn-ghost">← Back to Admin</a>
  </div>

  <% if (subs.length === 0) { %>
    <div class="empty-state">
      <div class="empty-icon">💳</div>
      <h3>No subscription requests yet.</h3>
    </div>
  <% } else { %>
    <div class="glass-card" style="overflow-x:auto">
      <table style="width:100%;border-collapse:collapse">
        <thead>
          <tr style="border-bottom:1px solid var(--border)">
            <% ['User','Method','Reference','Status','Submitted','Expires','Actions'].forEach(h => { %>
              <th style="padding:12px;text-align:left;color:var(--cyan);font-family:'Orbitron',sans-serif;font-size:.7rem"><%= h %></th>
            <% }) %>
          </tr>
        </thead>
        <tbody>
          <% subs.forEach(sub => { %>
            <tr style="border-bottom:1px solid rgba(255,255,255,.05)">
              <td style="padding:12px">
                <div style="color:#fff;font-weight:600"><%= sub.subscriber?.username || '—' %></div>
                <div style="font-size:.8rem;color:var(--text-muted)"><%= sub.subscriber?.email || '' %></div>
              </td>
              <td style="padding:12px">
                <% if (sub.paymentMethod === 'paypal') { %>
                  <span style="padding:2px 8px;border-radius:20px;font-size:.7rem;font-family:'Orbitron',sans-serif;background:rgba(124,58,237,.2);color:#c084fc">PayPal</span>
                <% } else { %>
                  <span style="padding:2px 8px;border-radius:20px;font-size:.7rem;font-family:'Orbitron',sans-serif;background:rgba(0,212,255,.1);color:var(--cyan)">GCash</span>
                <% } %>
              </td>
              <td style="padding:12px;font-family:monospace;color:var(--cyan);font-size:.85rem"><%= sub.gcashRef %></td>
              <td style="padding:12px">
                <% const colors = {pending:'rgba(251,191,36,.15)',active:'rgba(0,212,255,.15)',expired:'rgba(255,100,100,.15)'}; %>
                <% const textColors = {pending:'#fbbf24',active:'var(--cyan)',expired:'#ff6b6b'}; %>
                <span style="padding:3px 10px;border-radius:20px;font-size:.75rem;font-family:'Orbitron',sans-serif;
                             background:<%= colors[sub.status] %>;color:<%= textColors[sub.status] %>">
                  <%= sub.status.toUpperCase() %>
                </span>
              </td>
              <td style="padding:12px;color:var(--text-muted);font-size:.85rem">
                <%= new Date(sub.createdAt).toLocaleDateString('en-US', {year:'numeric',month:'short',day:'numeric'}) %>
              </td>
              <td style="padding:12px;color:var(--text-muted);font-size:.85rem"><%= sub.endDate || '—' %></td>
              <td style="padding:12px">
                <div style="display:flex;gap:.5rem">
                  <% if (sub.status === 'pending') { %>
                    <form action="/admin/subscriptions/<%= sub.id %>/approve" method="POST">
                      <button type="submit" class="btn btn-sm btn-primary">Approve</button>
                    </form>
                  <% } %>
                  <% if (sub.status === 'active') { %>
                    <form action="/admin/subscriptions/<%= sub.id %>/revoke" method="POST"
                          onsubmit="return confirm('Revoke this subscription?')">
                      <button type="submit" class="btn btn-sm" style="background:rgba(255,60,60,.15);color:#ff6b6b;border:1px solid rgba(255,60,60,.3)">Revoke</button>
                    </form>
                  <% } %>
                </div>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  <% } %>
</div>

<%- include('partials/footer') %>
